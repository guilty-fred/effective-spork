import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "no.guilty.fred",
  appName: "vipps-login-mvp",
  webDir: "dist",
  bundledWebRuntime: false,
};

export default config;
